<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Liife</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body class="background-image">
    <div class="navbar navbar-expand-sm navbar-light navbar-lewagon <%= "navbar-fixed" if params[:action] == "home" %>">
        <%= image_tag "https://res.cloudinary.com/debjoekzj/image/upload/v1574182391/Liife_Logo_1_iv03ex.png", class: "nav-image" %>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Create Lesson +", new_lesson_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Messages", "#", class: "nav-link" %>
      </li>
          <% if user_signed_in? %>
            <li class="nav-item-login"><%= link_to("Logout", destroy_user_session_path, method: :delete) %></li>
          <% else %>
            <li class="nav-item-login"><%= link_to("Login", new_user_session_path) %></li>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</div>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
